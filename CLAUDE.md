# HR Data 원큐 Agent Hands-on 문서 작업 규칙

이 문서는 `handson/HR_Data_OneQ_Agent_Hands-on_Guide.html`을 수정할 때 반드시 지켜야 하는 규칙입니다.  
가장 중요한 기준은 **선 맞춤, 계층 구조, 시각적 일관성**입니다.

---

## 1. 최우선 원칙

- 화면의 모든 요소는 기준선이 맞아야 한다.
- 텍스트, 번호, 표, 코드 블록, 이미지, 버튼 라벨은 같은 유형끼리 시작 위치와 높이를 맞춘다.
- 사용자는 특히 align을 매우 중요하게 본다. 애매하면 “보기 좋은가?”보다 “선이 맞는가?”를 먼저 확인한다.
- 새 디자인을 만들기보다 기존 페이지에 이미 있는 패턴을 재사용한다.
- 수정은 작게 하되, 같은 유형의 요소는 한 번에 일괄 수정한다.

---

## 2. 프로젝트 구조

배포 기준 구조는 아래를 기본으로 한다.

```text
handson/
  files/
    Knowledge_역량항목_채점기준.xlsx
    Knowledge_포지션별_요구역량.xlsx
    skchem_result_template.docx
  handson/
    HR_Data_OneQ_Agent_Hands-on_Guide.html
    css/
      style.css
    js/
      main.js
    images/
```


삭제하면 안 되는 것:

- `handson/`
- `handson/css/`
- `handson/js/`
- `handson/images/`
- GitHub Pages에서 다운로드 링크로 제공할 `files/`의 최종 3개 파일

---

## 4. LNB 규칙

### 제목 영역

- LNB 제목은 `HR Data 원큐 Agent 핸즈온`으로 표시한다.
- 제목 앞 로고는 `images/copilot-cropped.png`를 사용한다.
- 로고와 텍스트는 하나의 덩어리로 보고 박스의 정중앙에 배치한다.
- 로고와 텍스트 사이 간격은 좁게 유지하되, 너무 붙지 않게 한다.
- 제목 박스 배경은 로고 원본 배경색과 어울리는 연한 베이지 톤을 사용한다.

### 메뉴 계층

- `개요`는 Part 타이틀과 같은 위계로 보이게 한다.
- `Part`는 가장 큰 메뉴 그룹이다.
- `토픽`, `흐름`은 Part 아래의 구분 라벨이다.
- Step 메뉴는 같은 깊이로 정렬한다.
- Step 하위 메뉴는 부모 Step의 본문 텍스트 시작점과 맞춘다.
  - 예: `Step 15. 변수 초기화 - 응답결과` 아래의 `주요역량`, `상위역할`, `종합의견`은 `변수`의 `변` 글자 시작 위치에 맞춘다.
- `개요` 하위 메뉴는 Step 메뉴 시작선과 자연스럽게 맞춘다.

### Active 표시

- 현재 위치 메뉴는 하늘색 배경만 표시한다.
- 왼쪽 파란 막대는 사용하지 않는다.
- LNB active 상태는 본문에서 실제로 보이는 섹션 기준으로 자동 변경되어야 한다.
- 본문이 다음 Step으로 넘어왔는데 LNB가 이전 Step에 남아 있으면 오류다.

---

## 5. 본문 구조 규칙

- 모든 Step 제목은 흰색 섹션 카드 위에 있어야 한다.
- Step 제목 밑 설명문도 회색 작업 카드 안이 아니라 흰색 섹션 카드 위에 둔다.
- 회색 작업 카드는 실제 조작 절차 묶음에만 사용한다.
- 설명문은 “무엇을 하는가, 왜 하는가, 어떻게 쓰이는가”를 짧고 쉽게 설명해야 한다.
- 하위 작업 카드의 제목은 구체적으로 쓴다.
  - 예: `변수 초기화 : 식(fx)삽입 - 주요역량, 상위역할, 종합의견`
- 이미지는 가능하면 해당 Step 설명 직후, 조작 문장보다 먼저 배치한다. 단, 문맥상 문장 뒤가 더 자연스러우면 기존 패턴을 따른다.

---

## 6. CSS 작성 규칙

- 모든 스타일은 `handson/css/style.css`에서 관리한다.
- HTML 인라인 스타일은 사용하지 않는다.
- 같은 유형의 요소는 같은 클래스를 사용한다.
- 임시로 특정 요소만 맞추는 CSS를 만들지 않는다. 필요한 경우 의미 있는 클래스를 만든다.
- CSS 값은 기존 변수와 패턴을 우선 사용한다.
- 색상, border-radius, padding, gap은 기존 디자인 토큰과 가까운 값을 사용한다.
- 새 색상을 추가할 때는 전체 페이지 톤과 충돌하지 않는지 확인한다.

---

## 7. Align 세부 규칙

- 번호 원형, 본문 텍스트, 코드 블록, 표, 이미지의 시작선이 서로 어긋나지 않게 한다.
- `step-number` 오른쪽의 본문은 같은 Step 안에서 항상 같은 x축에 놓인다.
- inline copy box는 앞뒤 텍스트와 수직 가운데 정렬한다.
- 버튼 라벨 `[ ]`은 텍스트 줄 높이와 맞게 중앙 정렬한다.
- 표는 카드 안쪽 여백과 정확히 맞춘다.
- 표 바깥 테두리가 주변 카드보다 튀어나오면 안 된다.
- 이미지와 표는 같은 카드 안에서 좌우 여백 기준이 일관되어야 한다.
- 하위 메뉴/하위 항목은 너무 깊거나 얕지 않게 부모 텍스트 시작 위치를 기준으로 잡는다.

---

## 8. 버튼 라벨과 코드 표기

### 버튼/클릭 대상

- 클릭해야 하는 UI는 하늘색 배경의 `[버튼명]` 스타일을 사용한다.
- 예: `[저장]`, `[게시]`, `[+ 새 단계]`, `[JSON 구문 분석]`
- 클릭 대상인데 plain text로 두면 안 된다.

### code 표기

- 변수명, 시스템 값, JSON 필드, 고정값은 `<code>` 형태를 사용한다.
- 예: `<code>응답결과</code>`, `<code>승인여부</code>`, `<code>true</code>`
- 사용자가 그대로 입력하는 긴 문장이나 복사 가능한 수식은 `<code>`가 아니라 검정색 copy box를 사용한다.

---

## 9. 검정색 Copy Box 규칙

- 복사 가능한 값은 검정색 배경의 copy box로 표시한다.
- 모든 copy box는 동일한 높이, padding, 버튼 크기, 폰트 크기를 유지한다.
- copy box의 상하 여백은 과하게 크면 안 된다. 한 화면에 많은 줄이 보여야 한다.
- copy box 안의 `[복사]` 버튼은 클릭 후 `[복사됨]`으로 바뀌었다가 다시 `[복사]`로 돌아와야 한다.
- 같은 유형의 copy box는 반드시 동일한 동작과 크기를 가져야 한다.
- 수식은 copy box로 표시하고 `[복사]` 버튼을 둔다.
- 동적 데이터 값은 짧은 경우 `<code>`로 표시할 수 있다.

---

## 10. 표 규칙

- 표는 셀 높이를 과하게 키우지 않는다.
- 행 구분선은 명확하게 보이게 한다.
- 여러 개 묶음을 보여주는 표는 그룹 사이 구분선을 조금 더 강하게 둔다.
- 표의 모서리는 과하게 둥글게 하지 않는다.
- 불필요한 박스 중첩을 만들지 않는다.
- 파일명 컬럼처럼 기준 역할을 하는 컬럼에는 배경색을 유지해도 된다.
- `구분` 컬럼처럼 짧은 값이 들어가는 컬럼은 너무 넓게 잡지 않는다.
- 긴 값이 들어가는 컬럼에 공간을 더 준다.
- 많은 행이 있는 표는 compact하게 만들어 한 화면에 최대한 많은 정보가 보이게 한다.

---

## 11. 이미지 규칙

- 이미지는 실제 사용자가 확인해야 하는 UI를 가리지 않아야 한다.
- 이미지 hover 시 “클릭하여 확대” 같은 오버레이 텍스트는 사용하지 않는다.
- hover 시 커서 또는 확대 느낌만 제공한다.
- 이미지 테두리는 필요한 경우에만 사용한다.
- 목표/개요 이미지처럼 다이어그램성 이미지는 불필요한 테두리를 제거한다.
- 빨간 박스는 사용자가 클릭하거나 확인해야 하는 핵심 UI에만 표시한다.
- 영상에서 추출한 이미지는 버튼 위치가 명확할 때만 빨간 박스를 넣는다.
- 버튼이나 라벨이 불명확하면 문장에 `[확인 필요: 버튼명]`으로 표시한다.

---

## 12. 접기/펼치기 버튼 규칙

- 보조 참고 자료는 큰 영역을 차지하지 않도록 접기/펼치기 형태로 둘 수 있다.
- 메인 Step의 스키마/샘플처럼 사용자가 자주 열어야 하는 영역은 명확한 action button 스타일을 사용한다.
- 참고용 보조 영역은 과한 색을 쓰지 않고 조용한 스타일을 사용한다.
- 버튼 텍스트와 꺾쇠 아이콘은 수직 중앙 정렬되어야 한다.

---

## 13. Footer와 저작권

- LNB에는 저작권 문구를 넣지 않는다.
- 저작권은 본문 하단 footer에만 둔다.
- footer 문구는 눈에 띄지 않게 회색으로 표시한다.
- 현재 문구:

```text
© 2026 본 Hands-on 콘텐츠의 저작권은 SK주식회사 AX에 있습니다.
```

---

## 14. 내용 작성 규칙

- 설명은 쉬운 말로 쓴다.
- 기술 용어를 쓸 때는 바로 뒤에 이유를 설명한다.
- 예: “JSON 구문 분석은 JSON 문자열을 스키마 기준으로 해석해 이후 단계에서 선택 가능한 동적 콘텐츠로 만드는 단계입니다.”
- “무엇을 클릭한다”와 “왜 그렇게 한다”를 섞지 않는다.
- 먼저 개념을 설명하고, 그 다음 조작 단계를 번호로 안내한다.
- Step 설명문은 너무 길지 않게 한다. 필요한 경우 요약을 사용한다.

---

## 15. 검수 체크리스트

수정 후 반드시 확인한다.

1. LNB active 메뉴가 현재 본문 위치와 일치하는가?
2. LNB 제목 박스에서 로고+텍스트 덩어리가 중앙에 있는가?
3. LNB 하위 메뉴 들여쓰기가 계층별로 자연스러운가?
4. Step 제목과 설명문이 흰색 카드 위에 있는가?
5. 회색 작업 카드는 실제 절차 묶음에만 쓰였는가?
6. copy box 높이와 `[복사]` 버튼 크기가 전체적으로 같은가?
7. 표의 셀 높이와 행 구분선이 일관적인가?
8. 이미지가 문장 흐름에 맞는 위치에 있는가?
9. 불필요한 테두리, 박스 중첩, 장식이 없는가?
10. 파일명과 문서 표기가 `Hands-on` 기준을 따르는가?

---

## 16. 작업 방식

- 수정 전 관련 HTML/CSS/JS를 먼저 검색한다.
- 같은 유형의 문제가 있으면 한 곳만 고치지 말고 전체 패턴을 확인한다.
- 수동 파일 수정은 `apply_patch`를 사용한다.
- 삭제 작업은 배포 구조를 확인한 뒤 진행한다.
- 수정 후 최소한 아래를 실행한다.

```powershell
node --check "C:\Users\gram\Desktop\work\99 회사업무\handon\handson\js\main.js"
```

가능하면 브라우저에서 직접 열어 LNB, 표, copy box, footer를 육안 확인한다.
